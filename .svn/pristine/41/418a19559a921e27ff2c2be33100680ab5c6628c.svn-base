from feature.base import PersistentFeature

from feature.error import *

class Vol_Tick(PersistentFeature):
    description = 'tick close price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')


class Ask_Vol_Tick1(PersistentFeature):
    description = 'tick close price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')


class Ask_Vol_Tick2(PersistentFeature):
    description = 'tick close price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')


class Ask_Vol_Tick3(PersistentFeature):
    description = 'tick close price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')


class Ask_Vol_Tick4(PersistentFeature):
    description = 'tick close price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')


class Ask_Vol_Tick5(PersistentFeature):
    description = 'tick close price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')




class Bid_Vol_Tick1(PersistentFeature):
    description = 'tick close price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')


class Bid_Vol_Tick2(PersistentFeature):
    description = 'tick close price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')


class Bid_Vol_Tick3(PersistentFeature):
    description = 'tick close price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')


class Bid_Vol_Tick4(PersistentFeature):
    description = 'tick close price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')


class Bid_Vol_Tick5(PersistentFeature):
    description = 'tick close price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')



