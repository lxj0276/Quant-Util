from feature.base import PersistentFeature

from feature.error import *



class Ask_Price_Tick1(PersistentFeature):
    description = 'tick  price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')

class Ask_Price_Tick2(PersistentFeature):
    description = 'tick  price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')



class Ask_Price_Tick3(PersistentFeature):
    description = 'tick  price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')



class Ask_Price_Tick4(PersistentFeature):
    description = 'tick  price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')



class Ask_Price_Tick5(PersistentFeature):
    description = 'tick  price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')


class Bid_Price_Tick1(PersistentFeature):
    description = 'tick  price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')


class Bid_Price_Tick2(PersistentFeature):
    description = 'tick  price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')


class Bid_Price_Tick3(PersistentFeature):
    description = 'tick  price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')


class Bid_Price_Tick4(PersistentFeature):
    description = 'tick  price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')


class Bid_Price_Tick5(PersistentFeature):
    description = 'tick  price feature'

    granularity = 'tick'

    def _create_feature(self, instrument_id, time_range):
        raise OriginalFeatureError('Can not create original features')






